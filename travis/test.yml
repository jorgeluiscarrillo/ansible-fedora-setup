---
- hosts: all

  pre_tasks:
    - name: Ensure build dependencies are installed
      dnf:
        name: [ '@Development tools', 'tar', 'unzip', 'sudo', 'which' ]
        state: installed

  roles:
    - bootstrap_role
